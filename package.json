{
  "name": "eve-frontier-baseops",
  "version": "1.0.0",
  "private": true,
  "description": "Frontier BaseOps operator console for EVE Frontier bases",
  "type": "module",
  "scripts": {
    "authorise-gate-extension": "tsx ts-scripts/smart_gate_extension/authorise-gate-extension.ts",
    "authorise-storage-unit-extension": "tsx ts-scripts/smart_gate_extension/authorise-storage-unit-extension.ts",
    "build": "pnpm build:dapp",
    "build:dapp": "pnpm --dir dapps build",
    "build:smart-gate": "sui move build -e testnet --path move-contracts/smart_gate_extension",
    "build:storage-unit": "sui move build -e testnet --path move-contracts/storage_unit_extension",
    "collect-corpse-bounty": "tsx ts-scripts/smart_gate_extension/collect-corpse-bounty.ts",
    "configure-rules": "tsx ts-scripts/smart_gate_extension/configure-rules.ts",
    "dev:dapp": "pnpm --dir dapps dev",
    "dev": "pnpm dev:dapp",
    "docker:dev": "cd docker && docker compose run --rm --service-ports sui-dev",
    "flow:smart-gate": "pnpm configure-rules && pnpm authorise-gate-extension && pnpm authorise-storage-unit-extension && pnpm issue-tribe-jump-permit && pnpm jump-with-permit && pnpm collect-corpse-bounty",
    "fmt:check": "prettier --check \"ts-scripts/**/*.ts\"",
    "fmt:move": "sui move fmt --path move-contracts/smart_gate_extension && sui move fmt --path move-contracts/storage_unit_extension",
    "fmt:ts": "prettier --write \"ts-scripts/**/*.ts\"",
    "install:dapp": "pnpm --dir dapps install --ignore-workspace",
    "install:zklogin": "pnpm --dir zklogin install --ignore-workspace",
    "issue-tribe-jump-permit": "tsx ts-scripts/smart_gate_extension/issue-tribe-jump-permit.ts",
    "jump-with-permit": "tsx ts-scripts/smart_gate_extension/jump-with-permit.ts",
    "lint:dapp": "pnpm --dir dapps lint",
    "lint:move": "sh scripts/lint-move.sh",
    "publish:smart-gate:localnet": "cd move-contracts/smart_gate_extension && sui client test-publish --build-env testnet --pubfile-path ../../deployments/localnet/Pub.localnet.toml",
    "publish:smart-gate:testnet": "cd move-contracts/smart_gate_extension && sui client publish -e testnet",
    "setup:world": "sh setup-world/setup-world.sh",
    "type-check": "tsc --noEmit",
    "type-check:dapp": "pnpm --dir dapps exec tsc --noEmit",
    "zklogin": "pnpm --dir zklogin zklogin"
  },
  "dependencies": {
    "@mysten/sui": "^2.0.0",
    "@noble/hashes": "^1.8.0",
    "dotenv": "^17.2.3"
  },
  "devDependencies": {
    "@types/node": "^24.10.4",
    "prettier": "^3.3.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  },
  "packageManager": "pnpm@9.12.3",
  "engines": {
    "node": ">=18.x",
    "pnpm": ">=9.x"
  }
}
